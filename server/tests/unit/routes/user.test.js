/* eslint-disable no-undef */
/* eslint-disable @typescript-eslint/no-var-requires */
// eslint-disable-next-line @typescript-eslint/no-var-requires
var assert = require('assert');
var describe = require('node:test');

describe('user test', () => {
  assert.equal(1, 1);
});
